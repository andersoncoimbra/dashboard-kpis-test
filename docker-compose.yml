version: '3'
services:
  api_laravel:
      container_name: API_LARAVEL
      build:
        context: laravel
        dockerfile: Dockerfile
      ports:
        - 8888:8888
      volumes:
        - ./laravel:/laravel
      command: composer run startup
      networks:
        - rede
networks:
  rede:
    driver: bridge